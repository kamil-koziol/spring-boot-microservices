import { FilterTrainingsPipe } from './filter-trainings.pipe';

describe('FilterTrainingsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTrainingsPipe();
    expect(pipe).toBeTruthy();
  });
});
